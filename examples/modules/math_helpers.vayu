PI = 3.14159

def square(x: int) -> int:
    return x * x

def cube(x: int) -> int:
    return x * x * x

def greet(name: str) -> str:
    return "Hello, " + name + "!"

def circle_area(r: float) -> float:
    return PI * r * r